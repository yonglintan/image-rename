[project]
name = "image-rename"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yonglin", email = "yonglintan@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
]

[project.scripts]
image-rename = "image_rename:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.3",
]
